<div class="col-lg-12">
  <h2><%= link_to post.title, post %></h2>
  <%= render 'categories/category', collection: post.categories, as: :category %>
  <p><%= truncate(post.body, length: 100) %></p>
  <p><%= link_to 'Читать далее', post %></p>

  <br>

</div>
