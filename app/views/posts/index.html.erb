

<h1>
  <% if @category %>
      <%= 'Список публикаций категории '+@category.name %>
  <% else %>
      Список публикаций
  <% end %>
</h1>

<% if @posts %>
    <%= render @posts %>
<% end %>

<br>

<%= link_to 'Новая публикация', new_post_path, class: 'btn btn-large' if user_signed_in?  %>

